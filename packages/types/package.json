{
  "name": "@acme/types",
  "version": "0.0.0",
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "tsup src/index.ts --format esm,cjs --dts --watch",
    "build": "tsup src/index.ts --format esm,cjs --dts"
  },
  "devDependencies": {
    "tsconfig": "*",
    "tsup": "^6.6.3"
  }
}
